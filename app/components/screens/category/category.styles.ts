import styled from 'styled-components/native'

const Title = styled.Text`
	font-size: 26px;
`

const Options = styled.View`
	flex-direction: row;
	justify-content: space-between;
	margin-top: 10px;
`

const FilterButton = styled.Pressable``

export default { Title, Options, FilterButton }
